# Used only by Astro on the server/build to authenticate GitHub REST API requests.
# Create a fine-grained token with read-only Metadata access to the public repositories used by the portfolio.
GITHUB_TOKEN=
